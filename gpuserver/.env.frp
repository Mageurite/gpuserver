# GPU Server 环境变量配置
# 用于 frp 反向隧道方案

# ============================================
# API 服务配置
# ============================================
MANAGEMENT_API_HOST=0.0.0.0
MANAGEMENT_API_PORT=9000

# ============================================
# WebSocket 配置
# ============================================
WEBSOCKET_HOST=0.0.0.0
WEBSOCKET_PORT=9001

# WebSocket URL（前端连接使用）
# 部署时修改为 Web Server 的 IP 或域名
# 因为通过 frp 转发，前端需要连接到 Web Server 的 9001 端口
WEBSOCKET_URL=ws://YOUR_WEB_SERVER_IP:9001

# ============================================
# GPU 配置
# ============================================
CUDA_VISIBLE_DEVICES=0

# ============================================
# 会话配置
# ============================================
MAX_SESSIONS=10
SESSION_TIMEOUT_SECONDS=3600

# ============================================
# frp 配置说明
# ============================================
# GPU Server 通过 frpc 连接到 Web Server 的 frps
# 所有流量通过 frp 反向隧道转发
#
# 流量路径：
# 1. Web Server 后端 → ws://localhost:9000 (frp 转发) → GPU Server:9000
# 2. 前端浏览器 → ws://WEB_SERVER_IP:9001 (frp 转发) → GPU Server:9001
